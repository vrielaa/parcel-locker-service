PORT=3000

DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASS=postgres
DB_NAME=parcel_locker

JWT_SECRET=local-dev-secret-change-me

# Only for Google Cloud SQL deployments:
# INSTANCE_CONNECTION_NAME=project:region:instance
